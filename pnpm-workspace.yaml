# Allow these native packages to run their install/build scripts so a fresh
# `pnpm install` produces a working better-sqlite3 binary (pnpm v10 blocks
# build scripts by default).
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
